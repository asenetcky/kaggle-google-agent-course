# Google Agent Course


```bash
uvx edit --sandbox prompt-to-action.py
```

## ADK + CLI + WebGUI
using the web ui:

```bash
adk create sample-agent --model gemini-2.5-flash-lite --api_key $GOOGLE_API_KEY
```

```bash
adk web sample-agent --port=8085
```